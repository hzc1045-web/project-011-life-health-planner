# MVP 测试报告

测试日期：2026-09-03

## 已通过

- Android release 编译、R8 压缩、资源收缩与 release lint。
- Android JVM 单元测试 13 项：时间冲突、周期边界、预算、Schema 字段、紧急风险、健康硬约束、用药重复/过敏、数据最小化、反馈负荷调整和 AES-256-GCM 备份完整性。
- Android instrumentation 测试 APK 编译通过；包含首次引导主要操作和安全边界的可见性检查。
- Windows Companion：Ruff 静态检查通过，Pytest 10 项通过。
- 接口测试覆盖 `store:false`、结构化计划、未授权、重放、紧急风险拦截、备份完整性和跨设备访问隔离。
- Windows 启动/状态/停止冒烟测试通过；`/status` 返回版本 0.1.0，未配置密钥时明确报告 AI 离线。
- Tailscale 默认安装路径探测通过；未登录时中转保持本地可用并明确提示配对离线。

## 环境验证

- Android Studio 2026.1.4.7 已安装。
- Temurin JDK 17.0.20 已安装。
- Android SDK Platform 35/36、Build Tools 35.0.0/36.0.0 和 ADB 37.0.1 已安装。
- Tailscale 1.102.3 已安装，尚未登录。

## 尚待真机验收

当前 ADB 没有连接 Android 设备，因此尚未把以下项目标记为通过：真机安装、系统生物识别、Health Connect 授权与撤销、真实日历读写、通知重启恢复、时区切换、二维码配对、AI 实际额度/模型调用、全新安装备份恢复和多尺寸无障碍检查。

这些项目需要用户登录 Tailscale、录入自己的 OpenAI API 密钥并连接手机后执行。未完成真机步骤不影响已生成 APK 的签名和可安装性，但不能替代最终端到端验收。
