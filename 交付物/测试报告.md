# MVP 测试报告

测试日期：2026-09-04

## 已通过

- Android 0.1.2 release 编译、R8 压缩、资源收缩与 lint 通过；正式 APK 已复制到交付目录。
- Android JVM 单元测试 13 项：时间冲突、周期边界、预算、Schema 字段、紧急风险、健康硬约束、用药重复/过敏、数据最小化、反馈负荷调整和 AES-256-GCM 备份完整性。
- Android instrumentation 测试 APK 编译通过；包含首次引导主要操作和安全边界的可见性检查。
- Android Lint 通过：0 errors，21 warnings；警告为可用依赖更新、最低 SDK 下的旧资源限定符和 KTX 建议，本版为降低升级风险暂不改动。
- 正式 APK 清单验证通过：包名 `com.project011.lifehealthplanner`、`versionCode=3`、`versionName=0.1.2`、`minSdk=29`，应用名为“人生健康规划助手”。
- 正式 APK v2 签名验证通过；证书 SHA-256 为 `EE2BF508C03934EC5DDEE42234C247D80BD5EBAB92A5283D116EA4846FC431AF`，文件 SHA-256 为 `DA8CB210573A49CD1B7E73DFFC3B46C098B806FF48CCD7965C414EC10BA027B1`。
- Windows Companion：Ruff 静态检查通过，Pytest 17 项通过。
- 接口测试覆盖 AI小站 `store:false`、DeepSeek Chat Completions JSON 模式、结构化计划、计划条目上限、AI 超时、未授权、重放、紧急风险拦截、备份完整性和跨设备访问隔离。
- DeepSeek 官方真实调用通过：`deepseek-v4-pro` 在关闭思考模式后返回完整且通过 Schema 校验的 14 项周计划；服务端超时为 180 秒，Android 读取超时为 210 秒。
- 新增提供商一致性测试：手机请求的 `X-AI-Provider` 与电脑当前提供商不一致时返回 409，且不会调用模型，避免健康数据在切换后静默发送给另一家。
- Windows 启动/状态/停止冒烟测试通过；`/status` 返回版本 0.1.2、当前提供商和密钥状态，未配置密钥时明确报告 AI 离线。
- Windows 启动与停止脚本已增加 PID 身份核验；实测可忽略被系统进程复用的陈旧 PID，并可正常完成中转停止、重启和健康检查。
- Tailscale 1.102.3 已登录并完成真机私网配对；电脑地址 `100.88.9.86`，Android 地址 `100.124.40.104`，Companion 通过 Tailnet HTTPS 端点提供服务。

## 环境验证

- Android Studio 2026.1.4.7 已安装。
- Temurin JDK 17.0.20 已安装。
- Android SDK Platform 35/36、Build Tools 35.0.0/36.0.0 和 ADB 37.0.1 已安装。
- Tailscale 1.102.3 已安装并登录，手机与电脑均在线。

## 尚待真机验收

已完成 0.1.1 APK 真机覆盖安装（用户确认）、Tailscale 配对和 DeepSeek 官方实际模型调用，原手机配对记录仍保留；0.1.2 APK 待用户覆盖安装。尚未把以下项目标记为通过：系统生物识别、Health Connect 授权与撤销、真实日历读写、通知重启恢复、时区切换、全新安装备份恢复和多尺寸无障碍检查。

DeepSeek 官方密钥已由用户录入 Windows 凭据管理器并通过真实调用验证；密钥未进入源码、日志、APK 或备份。未完成真机步骤不影响已生成 APK 的签名和可安装性，但不能替代最终端到端验收。
